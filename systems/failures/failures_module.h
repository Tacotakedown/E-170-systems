#ifndef FAILURES_MODULE_H
#define FAILURES_MODULE_H

enum class FailureType {
    TYPE_A, // Replace with actual failure types
    TYPE_B,
    TYPE_C,
    // Add more failure types as needed
    INVALID // Add an INVALID type for error handling or when no failure is detected
};
#endif // !FAILURES_MODULE_H

